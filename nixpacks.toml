# nixpacks.toml

[phases.setup]
nixpkgs = ["python312", "openssl", "pkg-config"]
aptPkgs = ["libssl-dev"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "prisma generate"
]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port 3000"